name: example
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  sqlite3_library_windows:
    path: ..
  sqlite3: ^1.1.2
  path_provider: ^2.0.2

flutter:
  uses-material-design: true